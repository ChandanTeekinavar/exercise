apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pojok-lelang.fullname" . }}-test-connection"
  labels:
    {{- include "pojok-lelang.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pojok-lelang.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
